# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# The gem's dependencies are specified in jekyll-simple-tab.gemspec
gemspec
